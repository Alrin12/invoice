import React, {Component} from 'react';
import styled from 'styled-components';
import {Container} from "common/layout/CommonLayout";

export default class MainContainer extends Component {
  render() {
    return (
      <Container>
        <h2>hihi</h2>
      </Container>
    )
  }
}